// Base layout for dashboard group — individual portals override with their own layout
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
